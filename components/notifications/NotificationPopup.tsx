// MOVED — file relocated to components/layout/NotificationPopup.tsx (ADR-012).
// Sole consumer is BellButton in components/layout/ — layout exempt per ADR-012.
// This stub exists because GitHub does not support file deletion via API.
// Remove on next repo cleanup pass.
export {}
