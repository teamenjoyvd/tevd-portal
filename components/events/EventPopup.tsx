// MOVED: EventPopup has been co-located to the calendar feature route.
// This file is a forwarding alias — delete it once the old path is confirmed unused.
// ADR-012: components scoped to a single route live in app/[route]/components/
export { default } from '@/app/(dashboard)/calendar/components/EventPopup'
