name: Type Check

on:
  push:
    branches: [main]

jobs:
  typecheck:
    name: Supabase types + tsc
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Generate fresh types
        run: |
          echo "=== Generating Supabase types ==="
          supabase gen types typescript \
            --project-id ynykjpnetfwqzdnsgkkg \
            > /tmp/supabase-fresh.ts
          echo "=== Types generated successfully ==="
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Diff types against committed file
        run: |
          echo "=== STEP: Supabase type drift check ==="
          if ! diff -u types/supabase.ts /tmp/supabase-fresh.ts; then
            echo ""
            echo "❌ FAILED: types/supabase.ts is stale."
            echo "Run: supabase gen types typescript --project-id ynykjpnetfwqzdnsgkkg > types/supabase.ts"
            echo "Then commit the updated file."
            exit 1
          fi
          echo "✅ PASSED: types/supabase.ts is up to date."

      - name: TypeScript check
        run: |
          echo "=== STEP: TypeScript check (tsc --noEmit) ==="
          npx tsc --noEmit
          echo "✅ PASSED: tsc --noEmit returned zero errors."
