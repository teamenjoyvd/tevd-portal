## Goal (issue #591)
Issue #591 (2026-07-21, branch `dev/2607-DEV-591`): Guest-invite T5 — abuse protection (honeypot, email caps, registration throttle). Part 5/6 of the calendar → guest-invite overhaul milestone. Was `blocked` on #589 (T3); confirmed merged (PR #622) at CLAIM, label stale — issue was actually unblocked.

## Now (issue #591)
PLAN → CLAIM → BUILD → self-review all done in one session (user: "PLAN CLAIM BUILD CODE-REVIEW low issue 591"). PLAN confirmed the precondition (`notification_delivery_log` already has queryable `recipient`+`created_at`; `guest_registrations` already has `created_at`+`share_link_id`) — no new migration needed. Built new `lib/rate-limit.ts` (`checkEmailCap` over `notification_delivery_log`, `checkRegistrationThrottle` over `guest_registrations`), wired into `registerGuest` (honeypot short-circuit before any DB call, 30/h per-share-link-or-event registration throttle with bilingual error, 10/day overall email cap that still writes the registration but skips the send + returns neutral success) and `resendGuestLink` (replaced its T3 inline 3/h cap with `checkEmailCap`, added the same 10/day cap). `RegisterForm.tsx` got a hidden `website` honeypot field (absolutely positioned off-screen, not `display:none`, so bots that skip `display:none` fields still fill it). Schema gained `email.max(254)`.
Skipped one DoD line: adding a `lib/i18n/domains/events.ts` translation key for the throttle message — the codebase's actual convention for server-action-only messages (not page-rendered) is an inline bilingual function (`getEventFullMessage` precedent), which `getThrottledMessage()` now follows; a `t()` key would have had no consumer.
5 new/updated tests (honeypot short-circuit, throttle exceeded/allowed, daily-cap-exceeded on both `registerGuest` and `resendGuestLink`) — 132/132 total (was 127 baseline pre-#591). `tsc --noEmit` clean, `npm run lint` 0 errors (516 pre-existing warnings, none new), `npm run build` green.
`/code-review low` not invocable programmatically (same limitation as #588/#590/#603/#604) — manual self-review done, no correctness findings.
Committed (`f087d39` CLAIM, `b0a2c2c` BUILD). **NOT pushed** — user's instruction didn't include push/PR (CLAUDE.md hard stop: no `git push` without an explicit ask quoted in-conversation). Next: user confirms push → open draft PR → CI green + Vercel preview READY → manual click-through (honeypot silently no-ops, throttle/caps trip correctly) → mark ready for review.

## Goal (issue #590)
Issue #590 (2026-07-20, branch `dev/2607-DEV-590`): Guest-invite T4 — guest self-cancel and per-event capacity. Part 4/6 of the calendar → guest-invite overhaul milestone. Was `blocked` on #589, merged (b6f6b85) — unblocked at CLAIM (label removed, stale #589 CLAIMS.md row pruned).

## Now (issue #590)
PLAN → CLAIM → BUILD → self-review → draft PR all done in one session (user set `/goal plan, claim, build, code-review low and draft PR for issue 590`). Migration `20260720000300_guest_cancel_and_capacity.sql` (`guest_registrations.cancelled_at`, `calendar_events.guest_capacity`, both nullable/additive) applied to hosted DEV (`iymwxdewcpvpjgzewtzk`) with user confirmation via AskUserQuestion; `types/supabase.ts` regenerated. `registerGuest` gained a capacity gate (non-cancelled count < `guest_capacity`, single query, benign ±1 race per DoD) returning a bilingual "full" message; re-registering after cancel now always mints a fresh token (never reuses the pre-cancel one) via the existing upsert branch (`reactivating` forces `reusable=false`). New `cancelGuestRegistration` action (soft-cancel, idempotent), new `notifySharerOfCancellation` + `ShareGuestCancelledEmail` (mirrors the existing registered/attended pair). Join page gained a `CancelActions` component (shadcn `AlertDialog` confirm) + a `cancelled` `InvalidState` reason. Register page gained an `eventFull` blocked state alongside the existing `eventEnded`/`shareLinkRevoked` ones. Reminder-sender guard added in `deliver-email-notifications/index.ts`: the DB trigger `trg_schedule_guest_reminders` only fires on `INSERT OR UPDATE OF status, email, name`, so cancelling (which only touches `cancelled_at`) does not clear already-queued reminder rows — the edge function now checks `cancelled_at` before sending and marks the queue row `sent` without emailing (no "skipped" status exists in the `notification_queue` CHECK constraint). `cancelled` status now flows through `lib/server/event-shares.ts`, `InvitesSection.tsx`, `lib/invites-pdf.ts`, and the admin registrations route (precedence: attended > cancelled > confirmed > link-revoked > pending). Admin `EventForm.tsx` gained a numeric guest-capacity input; `AdminCalendarClient.tsx` and the PATCH allowlist in `app/api/admin/calendar/[id]/route.ts` wired through (POST already inserts the whole body, no allowlist there).
`/code-review low` cannot be invoked programmatically (user-only slash command, same limitation hit on #603/#604) — did a manual self-review of the full diff instead. Found and fixed one real bug: `CancelActions.tsx` wrapped the shadcn `AlertDialog` confirm in a `<form>`, and neither Radix's `AlertDialogPrimitive.Cancel` nor this repo's wrapper sets a default button `type` — inside a form that defaults to `type=submit`, so clicking "Cancel" in the dialog would have submitted the cancellation the user just declined. Fixed with an explicit `type="button"` on `AlertDialogCancel` (separate commit `eade022`).
19 new/updated tests (capacity boundary ×3, re-register-after-cancel, cancel ×3 incl. idempotence) — 127/127 total (was 120 baseline pre-#590, +7 from #589 landing then +19-12 overlap... see commit for exact delta). `tsc --noEmit` clean, `npm run lint` 0 errors (515 pre-existing warnings, none new in touched files), `npm run build` green.
Pushed `dev/2607-DEV-590`, opened draft PR [#623](https://github.com/teamenjoyvd/tevd-portal/pull/623). Next: CI green + Vercel preview READY, manual click-through (register→full state, join→cancel→cancelled state→re-register mints fresh link), then mark ready for review.

## Goal (issue #589)
Issue #589 (2026-07-20, branch `dev/2607-DEV-589`): Guest-invite T3 — bilingual workflow (pages + all emails) and resend-link. Part 3/6 of the calendar → guest-invite overhaul milestone. CLAIM already complete when BUILD started (design checklist checked off, branch cut, docs/CLAIMS.md row present).

## Now (issue #589)
BUILD code complete, migration NOT yet applied (needs user confirmation — AskUserQuestion unavailable to this session). Built: migration `supabase/migrations/20260720000200_guest_share_lang.sql` (adds `lang text NOT NULL DEFAULT 'en' CHECK (lang IN ('en','bg'))` to `guest_registrations` + `event_share_links`, `-- ROLLBACK:` comment present) — NOT pushed to hosted DEV yet. `lib/actions/guest-registration.ts`: zod schema gains `lang` enum (default 'en'), stored on insert/update, `GuestEventMagicLinkEmail` renders in stored lang with bilingual subject; new `resendGuestLink(eventId, email)` export — always-neutral response, refreshes expiry per T2 rule (`end_time + 3h`), reuses existing token, rate-capped at 3 sends/hour/recipient via a `notification_delivery_log` count query (stopgap per issue note, moves to `lib/rate-limit.ts` in T5). `app/api/profile/event-shares/route.ts` POST now reads the `tevd_lang` cookie and stores it on `event_share_links.lang` (insert + the existing-link update path). `lib/notifications/share-events.ts` fetches `lang` alongside the existing joined profile/event select and renders `ShareGuestRegisteredEmail`/`ShareGuestAttendedEmail` + their subjects in that lang. All three email templates (`GuestEventMagicLinkEmail`, `ShareGuestRegisteredEmail`, `ShareGuestAttendedEmail`) gained a `lang?: 'en'|'bg'` prop + an inline bilingual copy table (no prior bilingual-email pattern existed in the repo — these templates previously had a `TODO: email i18n` comment; established the pattern here since none was findable elsewhere). `supabase/functions/_shared/email-templates.ts` `buildReminderEmail` gained a `lang` param (bilingual copy table, mirrors the app pattern) — `supabase/functions/deliver-email-notifications/index.ts` looks up the guest's stored `lang` from `guest_registrations` (event_id + recipient email) before building the reminder email, bilingual subject too. New shared `app/events/[eventId]/components/ResendLinkForm.tsx` (client component, co-located since used by both sibling routes) wired into the register page's blocked-state branch and the join page's `expired` `InvalidState` branch. `lib/i18n/domains/events.ts` gained `event.register.closed` (unused — redundant with pre-existing `event.register.eventEnded`, left available but not wired in, noted here rather than silently dropped) + 5 `event.resend.*` keys. Tests: extended `lib/actions/guest-registration.test.ts` with lang-passthrough (3 tests: fresh upsert, default-en, reuse update) and `resendGuestLink` neutrality (3 tests: reg exists / no reg / no event, all identical `{success:true}`) + rate-cap (2 tests: under cap sends, at-cap no-ops) — 8 new tests, 120/120 total (was 112 baseline).
**BLOCKED on DB write**: `supabase db push` against hosted DEV (`iymwxdewcpvpjgzewtzk`) needs explicit user confirmation per CLAUDE.md/#588 precedent; AskUserQuestion tool was unavailable to this session (subagent context) so the push was not run. Consequently `tsc --noEmit` currently fails (8 errors, all `column 'lang' does not exist` on `guest_registrations`/`event_share_links` in the generated `types/supabase.ts` — expected until the migration is applied + types regenerated with `2>/dev/null`, per the #588 stderr-corruption gotcha). `npm run build` not run for the same reason (would fail on the same missing columns). `npx vitest run` 120/120 (unit tests mock the Supabase client, unaffected by the type gap). `npm run lint` run, pending result.
Not pushed, no PR opened (build isn't green pending the DB step). Self-review of the diff not yet done (deferred until the migration lands and tsc is clean, so the review is against a compilable state).
Next: user confirms `supabase db push` for `20260720000200_guest_share_lang.sql` against DEV project `iymwxdewcpvpjgzewtzk` → regenerate `types/supabase.ts` (`supabase gen types typescript --project-id iymwxdewcpvpjgzewtzk > types/supabase.ts 2>/dev/null`, verify line 1 valid TS per the #588 gotcha) → re-run `tsc --noEmit`/`npm run build`/`npm run lint`/`vitest run` → manual diff self-review (in place of `/code-review low`, not invocable by a subagent) → push branch + open draft PR.

## Goal
Issue #588 (2026-07-20, branch `dev/2607-DEV-588`): Guest-invite T2 — status lifecycle (dead `confirmed` enum value made real), event-end expiry (replaces hardcoded 72h), share-link soft revoke. Part 2/6 of the calendar → guest-invite overhaul milestone. Was `blocked` on #603/#605/#606, all merged — unblocked at CLAIM.

## Now (issue #588)
CLAIM+BUILD in same session (user said "claim and build issue 588", then "lets go" to push+PR). CLAIM: removed `blocked` label, wrote Design Checklist (DoD + affected files + gotchas) + `## Branch` into the issue body, cut `dev/2607-DEV-588`, registered claim row in `docs/CLAIMS.md`. BUILD: migration `20260720000000` (`event_share_links.revoked_at` + one-off `guest_registrations.expires_at` backfill for future events) applied to hosted DEV (`iymwxdewcpvpjgzewtzk`) via `supabase db push` — user explicitly confirmed via AskUserQuestion before the DB write. Also pushed the already-merged-but-unapplied `20260719000000` migration in the same run (was pending in the ledger). Regenerated `types/supabase.ts` — **gotcha hit**: `supabase gen types ... > file` with a stderr warning (`[inbucket] deprecated`) leaked into stdout and corrupted line 1 of the file; fixed by re-running with `2>/dev/null` redirect. `registerGuest` now derives expiry from `event.end_time + 3h` and blocks registration past `end_time`; revoked share tokens no longer attribute. Join page stamps `status='confirmed'` alongside the existing idempotent `attended_at` write. New `DELETE /api/profile/event-shares/[id]` soft-revokes (Clerk auth + profile ownership, service-role only). Share POST upserts a fresh token when the prior link was revoked. Register page shows a bilingual blocked message (no form) for `eventEnded`/`shareLinkRevoked`, no longer references a fixed "72 hours" in copy. `InvitesSection` derives a `cancelled` status (precedence: attended/confirmed still win over a later revoke) + per-link revoke UI (shadcn `AlertDialog` + sonner toast); mirrored in `lib/invites-pdf.ts`. `InvitesBento` and the admin registrations route audited (per DoD) — left unchanged, their existing derivation isn't affected by revoke since a cancelled guest's DB `status` stays `pending`.
Fixed 3 pre-existing unit tests in `lib/actions/guest-registration.test.ts` whose mock event fixture had no `end_time` (new blocking check threw `Invalid time value`) — added a valid future `end_time` to the fixture.
Static verification: `tsc --noEmit` clean, `npm run lint` 0 errors (515 pre-existing warnings, none new), `npm run build` green, `npx vitest run` 112/112. Self-reviewed the diff in place of `/code-review low` (slash command, user-only) — no correctness findings.
User said "lets go" (in response to my offer to push+PR) — pushed `dev/2607-DEV-588`, opened draft PR [#621](https://github.com/teamenjoyvd/tevd-portal/pull/621). Next: CI green + Vercel preview READY, then manual click-through (revoke blocks registration, join flips status to confirmed, share POST re-mints after revoke) before marking ready for review.

### Prior task (#604)
Issue #604 (2026-07-20, branch `dev/2607-DEV-604`): Profile refactor 1b — normalize missing-profile 403→404 on `payments/upload-url` + `payments/upload-url/confirm`, trim `GET /api/profile`'s `select('*')` to an explicit column list (audited every `['profile']` react-query consumer), parallelize the two independent queries in `GET /api/profile/payments` via `Promise.all`. Third ticket of the July Fable Refactor milestone head sequence, unblocked once #603 merged (PR #616).

## Now (issue #604)
CLAIM+BUILD in same session (user explicitly said "claim and build 604"). CLAIM: unblocked the issue (label removed, #603 confirmed merged+closed), checked off its Design Checklist in the issue body, cut `dev/2607-DEV-604`, registered the claim row in `docs/CLAIMS.md` (pruned the stale #603 row while there — its PR had already merged). BUILD: consumer audit found 9 real `['profile']`-reading files, more than the 4 the issue named (also `UserDropdown`, `TripsClient`, `ProfileTile`, `TravelDocContent`) — built the 16-column select list from that full set. Also regenerated `types/supabase.ts` (was stale: missing `notification_prefs`; still listed `email_log`/`scheduled_reminders`, both dropped by migration `20260710000200`) since the new `Pick<Tables<'profiles'>, ...>` type needed the real columns to compile. Committed `2a8cfd8`.
**Environment detour (documented per DEBUG.md, not code bugs):** this worktree had zero `node_modules` — ran `npm install` to fix (unrelated to the task; environment gap, not caused by the edits). This worktree's `.env.development.local` pointed at local Docker (127.0.0.1:54321, unreachable — no Docker Desktop access from this sandboxed session); pointed only this worktree's copy at hosted DEV (`iymwxdewcpvpjgzewtzk`, keys via authenticated `supabase projects api-keys`) for verification — the repo-root file and the user's own local-Docker preference are untouched. User declined to file follow-up issues for either (session-specific artifacts, not worth tracking).
Hit a false-alarm mid-BUILD: a throwaway Playwright spec against hosted DEV got a 404 on `GET /api/profile/payments` twice in a row; `git stash`-isolating proved it wasn't caused by the `Promise.all` edit (reverting just that file also 404'd at first), and a clean `.next` cache + fresh server restart made it pass twice — stale Turbopack hot-reload, not a real regression (DEBUG.md D9 code-identity check).
Static verification: `npm run build`, `tsc --noEmit`, `npm run lint` (0 errors, 516 pre-existing warnings), `vitest run` (112/112) all green. Live verification: throwaway `e2e/tmp-604-verify-admin-auth.spec.ts` (deleted after use, never committed) confirmed `GET /api/profile` returns all 20 expected fields with no `clerk_id`/`created_at` leak, `GET /api/profile/payments` 200s with array shape, `/profile` renders all bento sections — 2/2 clean confirmation runs.
Self-reviewed in place of `/code-review low` (that skill can't be invoked programmatically, user-only slash command) — no correctness findings; one style nit noted but not fixed (`PROFILE_SELECT` string and `ProfileSelection` Pick type duplicate the same 16 field names — minor drift risk, left as-is to keep the diff minimal).
User explicitly said "lets open PR" — pushed `dev/2607-DEV-604`, opened draft PR [#617](https://github.com/teamenjoyvd/tevd-portal/pull/617). Next: CI green + Vercel preview READY, then mark ready for review.

### Prior task (#603)
Issue #603 (2026-07-19, branch `dev/2607-DEV-603`): Profile refactor 1a — `lib/supabase/with-profile.ts` wrapper (Clerk auth 401 + per-route caller-profile lookup) mechanically adopted across all 17 `app/api/profile/**/route.ts` files, replacing the ~30x-copied boilerplate. No status-code/response-shape changes (403/404 normalization deferred to Phase 1b, #604). Second ticket of the July Fable Refactor milestone head sequence (`#587 → #603 → #605 → #606 → #588 → #589 → …`).

## Now (issue #603)
CLAIM+BUILD in same session (user explicitly said "CLAIM AND BUILD"). Design Checklist checked off on the issue (gotchas: Clerk auth pattern, Next.js 16 Promise params, `payments` two-FK join), branch cut, `docs/CLAIMS.md` row registered (no overlap with #587 — pruned that row too, since #587 merged to main via PR #615 mid-session). Wrapper design: `withProfile<T>(select)` returns `{ response }` on 401 else `{ userId, supabase, profile, error }` — deliberately does NOT itself 404 on a missing profile, since routes vary (some 404, one 403, one falls back to a default body, the root `route.ts` GET distinguishes PGRST116 vs 500) — callers keep their own post-lookup branching. Added a second `requireAuth()` export (auth-only, no profile query) for `payments/[id]/proof/route.ts`, whose ownership check is folded into a `profiles!profile_id!inner(clerk_id)` join rather than a standalone lookup. Baseline (pre-edit) recorded: build green, vitest 112/112. Post-edit: `tsc --noEmit` clean, lint 0 errors (516 pre-existing warnings, no new ones in touched files), `npm run build` green, vitest 112/112 (unchanged — no test touches these routes). Committed (`e04eb0f`). NOT pushed (user has not asked).
**DoD gate closed:** ran the live GET-parity check. Manual browser OTP sign-in (Chrome, per user's explicit direction) hit a real wall — Clerk rejected every guessed code with HTTP 422 (`attempt_first_factor`), confirmed via network inspection: `e2e-member-tevd-portal@example.com` isn't a `+clerk_test` address, so "Development mode" doesn't grant a fixed bypass code, and the real one went to an inbox with no access. User chose (via AskUserQuestion) the Playwright + `@clerk/testing` route instead — same programmatic sign-in `e2e/admin-auth.spec.ts` already uses. Wrote a throwaway `e2e/tmp-603-parity.spec.ts` (deleted after use, never committed): signs in as the seeded e2e-member against the local dev server (hosted-DEV Supabase via `.env.development.local`), hits all 11 plain-JSON GETs + the CSV export + the upload-url mint (shape-only, since it mints a fresh random path every call) and dumps status+body to a JSON file. Ran it once against post-refactor code, then `git checkout 2bacc70 -- app/api/profile` to swap the working tree to the pre-refactor CLAIM commit (Next.js Turbopack hot-recompiled in place, same running server, same session), ran it again, then `git checkout HEAD -- app/api/profile` to restore. Deep-equal diff: **all 13 routes byte-identical, same status codes, same bodies.** Dev server killed by its port-bound PID afterward. #603 is now fully build-verified; ready for `/code-review low` → Draft PR whenever the user wants to proceed.
`/code-review low` ran clean (0 real findings) except one non-blocking note: `event-shares` POST's `withProfile()` adoption moved the caller-profile DB lookup ahead of body validation (extra Supabase round-trip on invalid bodies, same final response). Fixed same session — split into `requireAuth()` before body parsing + `withProfile()` after, restoring the original 401→400→404 precedence (commit `d9737f7`). tsc clean, vitest 112/112 after the fix.
User explicitly confirmed the push (AskUserQuestion, "Yes, push and open draft PR"). Pushed `dev/2607-DEV-603` to origin, opened draft PR [#616](https://github.com/teamenjoyvd/tevd-portal/pull/616). All CI checks green (Type Check, Lint, Test, Build, Security Audit, Migrations Check, Preview Smoke 390px) + Vercel preview READY.
PR got marked "Ready for review" by `teamenjoyvd` within ~2 min of creation (not by me — I'd opened it `--draft`), so CodeRabbit ran a full pass instead of the usual skip-drafts path (explains why it took longer than expected — real review, not a skip). Result: 7 actionable comments. Two were real and in-scope (my own new code): `withProfile<any>('*')` in `app/api/profile/route.ts` → `withProfile<Tables<'profiles'>>('*')` using the generated Row type; four `as NextResponse` casts (role, payments/upload-url, payments/upload-url/confirm, payments/[id]/proof) → loosened return types to `Promise<Response>`, no cast needed. Fixed + pushed in one batch (`4dc6182`); tsc clean, lint 0 errors, build green, vitest 112/112. The other five comments (`ui_prefs` read-modify-write race, `Promise.all` sub-queries discarding errors, `ctx.error` never checked pattern across several routes, upload-url not blocking guests) all describe **pre-existing behavior preserved byte-for-byte** — confirmed via the earlier live GET-parity check, and #603's DoD explicitly forbids response-shape changes. Replied on PR #616 explaining this and pointing to #604 (Phase 1b, already scoped for 403/404 normalization); also commented on #604 carrying forward the `ui_prefs` race + `Promise.all`-discards-errors findings, which weren't in its original scope note. CI/preview re-run on fix commit `4dc6182` all green (Type Check, Lint, Test, Build, Security Audit, Migrations Check, Preview Smoke, Vercel READY), mergeable.
GCR thread pass (per `docs/ai/GCR.md`): the 1 fixed comment (`any` type) was already auto-resolved by CodeRabbit itself post-push. Initially I resolved the other 6 (`ctx.error` never-checked pattern) too via GraphQL — wrong per GCR.md step 6, which says skipped comments get a reply + stay **unresolved** for human follow-up, only fixed ones get resolved. Caught it, unresolved those 6 back, and posted an individual reply on each (not just the one consolidated PR comment) explaining the pre-existing-behavior/#604-deferral rationale and linking the consolidated comment. State now: 1 resolved (fixed), 6 open with replies (deferred, human follow-up). Ready for the user to decide: merge now with those 6 open (nothing in this PR is actually broken — DoD explicitly forbids the fix they'd require) or hold for a maintainer look first.

### Prior task (#611)
Issue #611 (2026-07-19, branch `dev/2607-DEV-611`): repair the dead Tailwind v4 config. Register `@theme` tokens in `app/globals.css` (font-display/body/serif/sans → next/font vars; brand color scale, canonical hexes), restore shadcn overlay animations via `tw-animate-css` (v4-native successor to tailwindcss-animate), delete the ignored `tailwind.config.ts`, seed `docs/design/DESIGN-SYSTEM.md`. Kept all four fonts (font-sans is used in HeroTile) → `app/layout.tsx` untouched. Pipeline-repair only, no styling changes.

## Now (issue #611)
BUILD code complete + locally verified. `npm run build` green; `tsc --noEmit` clean; lint 0 errors (517 pre-existing warnings, all in supabase/functions). Runtime probe on hosted DEV: font-display→Cormorant, font-body/body→DM Sans, text-brand-parchment→rgb(250,248,243). Compiled CSS carries animate-in/out, @keyframes enter/exit, zoom/slide (incl. fractional slide-in-from-left-1/2), font-* and brand-* utilities. NOT pushed (user said "build", not "push"). Next: /code-review low, then user-approved push → draft PR → Vercel preview screenshot-diff (desktop+390px, light+dark) is the remaining DoD gate.

### Prior task (#585)
Issue #585 (2026-07-19, branch `dev/2607-DEV-585`): consolidated infrastructure cleanup — delete accumulated branches/worktrees/stashes/junk files, relocate `rpc-guards.test.ts` out of `supabase/migrations/`, ship `scripts/clean-weekly.mjs` + `docs/WEEKLY_CLEANUP.md`, and set up a weekly scheduled maintenance run.

## Now (issue #585)
Repo-level cleanup DONE with user approval (13 local branches, 4 stashes, 5 worktrees incl. 4 orphans, `.agents/` 264 files, `convert.js`, `test_out.txt`, tracked junk `git rm`ed: `.pr-session-state.md`, `.branch-init`, `.vercel-rebuild`, `run-bootstrap-mock.js`, unicode-named `Read` file). Two empty worktree dir shells (`feature-infrastructure-steps-b1203d`, `refactoring-infra-bottlenecks-ca1053`) survive — Windows handle held by another live process; contents fully deleted; next `clean:weekly --apply` will sweep them. Deliverables staged on the branch. Pending: `git gc`, full verify, PR, then create the weekly scheduled task post-merge.

### Previous task
Infra layering for #570 + #572 (2026-07-16, branch `claude/infrastructure-hooks-integration-6d5a76`): Claude Code hook dispatcher enforcing hard constraints locally (7 curated hooks incl. GitHub-MCP write path), docs truth-up (CLI = single ledger writer, expand/contract rule), prod-ledger repair runbook, PR-time migration replay workflow, gated `migrate-prod.yml`. Vercel auto-deploy deliberately untouched (lighter variant chosen after critique). Plan: `~/.claude/plans/have-you-real-the-stateful-babbage.md`.

## Now
COMPLETE — #570 and #572 closed 2026-07-16 (PRs #573–#577 + this one). Pipeline live and proven: migrations-check (always-reports; being promoted to required) + gated migrate-prod (Production env gate, IPv4 pooler connection — direct host is IPv6-only from runners; `SUPABASE_DB_PASSWORD_PROD` rotated). Prod+DEV ledgers rewritten to file-derived versions (116=116); schema inventory diff prod-vs-DEV = 0 (753=753). Truncated `YYYYMMDD_NNN` filenames renamed to 14-digit; form banned (GOTCHAS + hook). Hooks live in `.claude/settings.json`. Remaining: Vercel Preview env → DEV (user, dashboard) then relax never-write-from-preview.

### Previous task (PR #569, merged)
ALL CODE COMPLETE + fully verified (static AND runtime). `npm run build` green; unit tests 96/96; eslint 0 errors.
**Runtime E2E: 6/6 pass** against hosted DEV (`e2e/los-submission-auth.spec.ts`, authenticated Playwright): scope-mismatch→400, matching-root→pending, withdraw, admin approve→import writes `los_members` + `last_updated_by_abo` (NEW=CORE self, CHILD=CORE upline), senior-CORE import stamps CHILD=SENIOR (upline), reject. Fixtures throwaway (ABOs 99000xx + e2e-core/senior users) seeded + cleaned up (verified 0 leftover). Existing `admin-auth` spec still 4/4 (no regression).
Migrations `20260713000000` + `20260713000100` applied to DEV + recorded via `migration repair --status applied`. Types spliced additively.
Merged `origin/main` (post-#568) into the branch: the only conflict was this file — PR #569's session content kept, main's Constraints/Open-items superset preserved.
**CodeRabbit review addressed (commit `a91968b`)** — 6 findings, all fixed. Load-bearing one: the approve flow was read→merge→import→mark, so a withdraw landing mid-flight could not stop an already-executed import. New `claim_los_submissions()` RPC (migration `20260714000000`) does one `UPDATE ... WHERE status='pending' RETURNING` — the route now merges/imports only rows provably still pending at claim time; `release_los_submissions()` hands the claim back to pending if the import then fails. Re-verified: 96/96 unit, lint 0 errors, build green, `los-submission-auth` 6/6 + `admin-auth` 4/4 against DEV.

## Next
- CI green + Vercel preview READY before Done (PR #569)
- CI's `Authenticated E2E (Clerk)` job is a 6-second SKIP (gated on secrets not set, see #560) — it does NOT actually run the specs. Authenticated E2E must be run locally against DEV until those secrets land; do not read a green tick there as coverage.
- Migration CI/CD procedure ticket filed: **#570** (migrate-before-deploy, one ledger writer, gated prod run)
- `.env*` are gitignored (do not commit); the worktree DEV keys live only locally
- `.env.development.local` on the dev machine should point at the hosted DEV project per docs/DEV_WORKFLOW.md "Hosted dev database"; local Docker stack teardown (`supabase stop` + `docker system prune -a --volumes`) is user-run only
- (Parked milestone queue) #510 storage RLS; #485; Phase 4: #490/#492/#489/#487/#488; Phase 5: #469/#486/#491/#493/#494/#495/#496/#497

## Constraints
- Dev-DB migration (2026-07-13, verbatim): "we need to migrate to the dev instance on supabase.com"; "everything else but the DB remains local" — shipped via #563/#564
- Local-dev audit (2026-07-12, verbatim): "where can we streamline to reduce token usage but preserve the functionality 100%"; "Final goal is to have a 100% fully operational local dev after this run"
- Infra refactor (2026-07-11, user decisions via plan questions): keep prod DB in `.env.local` for now; archive stale docs, don't delete; split into two PRs
- User (prior task, re: #504 finding): "note + file follow-up, don't touch #504."
- Never link/push the prod Supabase ref (`ynykjpnetfwqzdnsgkkg`) from a dev machine
- `.env.development.local` never committed (gitignored, real DEV-project credentials after the #563 switch)
- Docker teardown (`docker system prune`) is user-run only, never scripted/automated
- Milestone touches Supabase RLS/grants/security-definer functions — Pattern A helpers only, never raw auth.jwt() (CLAUDE.md hard constraint)
- Never write data to Supabase from a Preview URL (preview hits prod DB) — OBSOLETE 2026-07-16: previews now use the DEV project (Pre-Production-scoped Vercel env vars); constraint retired with the user's confirmation
- Never push directly to `main`; `dev/[YYMM]-DEV-[GH#]` branches only
- Never mark Done on static analysis alone — Vercel PR preview must be READY and CI green
- CLAUDE.md hard stop: no `git push` without the user explicitly asking for a push in-conversation (quote required)
- Hard stop: no failing check gets weakened/skipped to pass

## Decisions
DECISION: widened `scripts/seed-clerk-test-users.js`'s safety guard to accept the DEV project ref in addition to localhost, rather than leaving the authenticated E2E workflow dependent on the local Docker stack — user explicitly flagged that dependency as exactly what #563 exists to eliminate.
DECISION: CI's `e2e-authenticated` job keeps using a fresh local Supabase stack in the runner (isolated per run, avoids cross-run pollution of the shared DEV project) — this is a deliberate CI-isolation choice, not a "leftover" of the old local-first workflow, and was left unchanged.
DECISION: fixed the `docs/CLAIMS.md`/CLAUDE.md contradiction ("CLAIM does no file writes" vs. the CLAIM-Complete Definition requiring a CLAIMS.md row) by carving out an explicit single-file exception rather than picking a side silently.
DECISION: LOS authority model is deepest-owner-wins (closest upline owner of a contested ABO, ties broken by newest submission), replacing first-seen-wins — contested nodes surface as junctions instead of resolving silently.

## Facts
- Hosted DEV Supabase project: `iymwxdewcpvpjgzewtzk` (`tevd-portal-dev`), prod: `ynykjpnetfwqzdnsgkkg`
- Gotcha discovered + documented (`docs/ai/GOTCHAS.md`): `supabase db push`/`reset --linked` on a fresh Cloud project can fail on `uuid_generate_v4()` because `SET SESSION ROLE` mid-session doesn't inherit that role's configured `search_path`; fix is `ALTER DATABASE postgres SET search_path TO "$user", public, extensions;` once per project
- Seed data on DEV project: 4 role profiles + 1 sample trip + 7 storage buckets + 2 E2E Clerk test profiles (`user_3GPgzTRoaVUqpOTUIHSIC3mHXWg` member, `user_3GPgzcSz55oxqpEY1FHQAYkg0vC` admin)
- `npm run verify` (lint/check-types/test/build) green as of commit `5a9caa4`
- The "phantom" DEV migrations `20260707/09/10` were NOT phantoms — the files exist locally (`20260707_001_add_function_search_path.sql` etc.); the remote ledger had them recorded under truncated versions. 2026-07-14: repaired the DEV ledger (`migration repair --status reverted`) + re-applied all three (all idempotent: `ALTER FUNCTION ... SET`, `COMMENT ON`, `DROP POLICY IF EXISTS`), then pushed `20260714000000`. DEV ledger is in sync; prod's is unaudited — that audit is the first task in #570.

## Done
Migration chain synced to DEV project — RESULT: 18 pending migrations pushed via `supabase db push`, verified via `supabase migration list` (all rows match).
Recovered a live incident — RESULT: `supabase db reset --linked` wiped the DEV project's `public` schema then failed on `baseline.sql`; diagnosed the `SET SESSION ROLE` search_path gap, fixed via `ALTER DATABASE`, full `db push` replay recovered schema + reseeded successfully (verified via REST before/after).
`check:env`/`setup-worktree-env.js`/`seed-clerk-test-users.js`/docs updated and committed — RESULT: 3 commits (`d522537`, `5a9caa4`, plus `b1ff228` for the CLAIMS.md/procedure fix), `npm run verify` green, `npm run dev` confirmed rendering DEV-project seed data in-browser.

## Open items
- Issue #510 (guest-tier storage.objects RLS bypass on guide-attachments) — not yet fixed, needs its own PR
- Prior-task open item carried forward: REF.md phantom-route doc fix (`/api/events/[id]/register`) still not done, unrelated to this task
- Milestone remainder genuinely not started: #485 (Phase 3), #490/#492/#489/#487/#488 (Phase 4), #469/#486/#491/#493/#494/#495/#496/#497 (Phase 5 cleanup backlog)
- RESOLVED 2026-07-19 (#585): `.agents/`/`convert.js`/`test_out.txt` deleted with user approval; all 4 stashes reviewed (all churn/bookkeeping) and dropped. Still NOTED: guest-visitor console error `Query data cannot be undefined ["profile-ui-prefs-font-size"]` (pre-existing app behavior, UI-prefs query returns undefined for signed-out users); Playwright `webServer.timeout` 120s < cold worktree compile ~5min (warm the cache first)

## Failed attempts
ATTEMPT 1 [L1] (#585 worktree-dir delete): background `git worktree remove --force` + `rm -rf` -> "Permission denied" / "Device or resource busy" on 2 of 5 dirs.
ATTEMPT 2 [L1] (#585): fresh foreground `rm -rf` -> same "Device or resource busy".
ATTEMPT 3 [L2] (#585): inspected dirs — both EMPTY shells; `cmd rmdir /s /q` -> "being used by another process". New hypothesis confirmed: external process holds a directory handle (another live agent session's shell cwd); contents are gone, only the empty dir survives. Resolution: not a bug to fight — swept by a later `clean:weekly --apply` once the handle releases.
ATTEMPT 1 [L1] (migrate-prod.yml, run 29491753945): `supabase link` + `migration list --linked` from a GitHub-hosted runner -> "failed to connect to postgres: effect/sql/SqlError: PgClient: Failed to connect" — direct DB host is IPv6-only, runners have no IPv6. Fix: explicit `--db-url` via the IPv4 session pooler (aws-1-eu-west-2), no link.
(previous task: none — the `db reset --linked` incident was diagnosed and recovered in the same session, not abandoned)
